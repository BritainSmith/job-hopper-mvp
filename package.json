{
  "name": "job-hopper",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "cd backend && npm run start:dev",
    "start": "cd backend && npm run start:prod",
    "build": "cd backend && npm run build",
    "test": "cd backend && npm test",
    "test:watch": "cd backend && npm run test:watch",
    "test:cov": "cd backend && npm run test:cov",
    "test:e2e": "cd backend && npm run test:e2e",
    "lint": "cd backend && npm run lint",
    "format": "cd backend && npm run format",
    "format:check": "cd backend && npm run format:check",
    "db:studio": "npx prisma studio",
    "db:generate": "npx prisma generate",
    "db:migrate": "npx prisma migrate dev",
    "db:migrate:status": "npx prisma migrate status",
    "db:reset": "npx prisma migrate reset",
    "setup": "echo \"Environment setup: Copy .env.example to .env and configure your variables\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@nestjs/throttler": "^6.4.0"
  }
}
